import styled from 'styled-components';

const ErrorMsg = styled.p`
	color: ${props => props.theme.wesblue};
	text-transform: uppercase;
	font-weight: bold;
	padding: 0;
	margin: 0;
`;

export default ErrorMsg;
